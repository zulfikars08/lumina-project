import { StoreFooter, StoreHeader } from '@/components/storefront';
import { AccountShell } from '@/components/account-shell';
import { requireUser } from '@/lib/auth/rbac';
import { supabaseAdmin } from '@/lib/supabase/server';
export default async function ProfilePage({searchParams}:{searchParams:Promise<{status?:string}>}){ const user=await requireUser(); const params=await searchParams; const {data:row}=await supabaseAdmin.from('users').select('password_changed_at,status').eq('id',user.id).single(); return <><StoreHeader/><AccountShell user={user}><h2 className="text-2xl font-semibold text-rose-950">Profile</h2>{params.status?<p className="mt-3 text-rose-700">Profile updated.</p>:null}<form action="/account/profile/update" method="post" className="mt-6 grid gap-4"><label>Name<input className="mt-1 w-full rounded-full bg-rose-50 px-4 py-3" name="name" defaultValue={user.name} required /></label><label>Email<input className="mt-1 w-full rounded-full bg-stone-100 px-4 py-3" value={user.email} readOnly /></label><p className="text-sm text-stone-500">Password changed: {row?.password_changed_at?'yes':'not yet'} · Status: {row?.status}</p><button className="rounded-full bg-rose-900 px-6 py-3 text-white">Save profile</button></form></AccountShell><StoreFooter/></>; }
